import { Edit, Trash2, Clock, Globe, Loader2 } from "lucide-react"

// Centralize icons so we can change the icon library later if needed
export const Icons = {
  Edit,
  Trash2,
  Clock,
  Globe,
  Loader2,
}

export default Icons
